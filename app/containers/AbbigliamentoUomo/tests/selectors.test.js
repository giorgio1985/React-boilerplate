// import { selectAbbigliamentoUomoDomain } from '../selectors';

describe('selectAbbigliamentoUomoDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
