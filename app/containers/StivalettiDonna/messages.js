/*
 * StivalettiDonna Messages
 *
 * This contains all the text for the StivalettiDonna container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.StivalettiDonna';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the StivalettiDonna container!',
  },
});
