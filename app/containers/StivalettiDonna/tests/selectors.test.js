// import { selectStivalettiDonnaDomain } from '../selectors';

describe('selectStivalettiDonnaDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
