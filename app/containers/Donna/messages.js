/*
 * Donna Messages
 *
 * This contains all the text for the Donna container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Donna';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Donna container!',
  },
});
