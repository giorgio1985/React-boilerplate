// import { selectOcchialiUomoDomain } from '../selectors';

describe('selectOcchialiUomoDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
