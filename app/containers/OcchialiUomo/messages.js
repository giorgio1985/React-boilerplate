/*
 * OcchialiUomo Messages
 *
 * This contains all the text for the OcchialiUomo container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.OcchialiUomo';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the OcchialiUomo container!',
  },
});
