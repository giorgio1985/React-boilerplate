/*
 * TShirtDonna Messages
 *
 * This contains all the text for the TShirtDonna container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TShirtDonna';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TShirtDonna container!',
  },
});
