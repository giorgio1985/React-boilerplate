/*
 * StivaliUomo Messages
 *
 * This contains all the text for the StivaliUomo container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.StivaliUomo';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the StivaliUomo container!',
  },
});
