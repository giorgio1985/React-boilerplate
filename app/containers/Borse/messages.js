/*
 * Borse Messages
 *
 * This contains all the text for the Borse container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Borse';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Borse container!',
  },
});
