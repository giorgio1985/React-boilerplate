// import { selectAccessoriNewsDomain } from '../selectors';

describe('selectAccessoriNewsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
