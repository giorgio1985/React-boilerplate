/*
 * AccessoriNews Messages
 *
 * This contains all the text for the AccessoriNews container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AccessoriNews';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AccessoriNews container!',
  },
});
