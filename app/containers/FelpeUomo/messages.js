/*
 * FelpeUomo Messages
 *
 * This contains all the text for the FelpeUomo container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.FelpeUomo';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FelpeUomo container!',
  },
});
