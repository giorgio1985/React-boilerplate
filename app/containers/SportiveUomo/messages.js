/*
 * SportiveUomo Messages
 *
 * This contains all the text for the SportiveUomo container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SportiveUomo';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SportiveUomo container!',
  },
});
