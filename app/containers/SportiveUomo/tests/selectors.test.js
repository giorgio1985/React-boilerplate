// import { selectSportiveUomoDomain } from '../selectors';

describe('selectSportiveUomoDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
