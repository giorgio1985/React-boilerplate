// import { selectStivaliDonnaDomain } from '../selectors';

describe('selectStivaliDonnaDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
