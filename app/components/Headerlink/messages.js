/*
 * Headerlink Messages
 *
 * This contains all the text for the Headerlink component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Headerlink';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Headerlink component!',
  },
});
